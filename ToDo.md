# MY TODO LIST

- Create markdown file that details what each script does
- Improve documentation comments in each scripts
- Make a driver script to run the entire pipeline
- Add how to run pipeline and driver instructions in the main README.md

# Branch Feature to implement:

- remove arguments in command line and use config.yaml for all settings
- create an oncotree.json to store the entire oncotree structure and store it in the repo
- create a script to find the closest oncotree code if an exact match is not found
- This will automate the process of creating the sample_info.tsv file
- lastly, for each datatype, combine all the samples from the sample_info.tsv into one master all_samples_info.tsv file which will be used to make the final data_clinical_sample.txt file

